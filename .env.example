# Session cookie signing secret
COOKIE_SECRET=

# Google OAuth credentials
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Postgres connection string (production only; dev uses sqlite via server.config.json)
DATABASE_URL=
