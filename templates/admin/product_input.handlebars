<div class="mb-2 form-group ">
    <label class="card_title">{{label}}</label>
    <input {{ disabled label }} name="{{ name }}" class="input_col"
        value="{{ data.value }}" />
    {{> admin/validation_messages data}}
</div>