node_modules/
dist/

# secrets — real values live only in the environment / local untracked files
# (production.server.config.json is tracked: it no longer holds secrets,
# and the app reads it directly at runtime — see src/config/index.ts)
*.env

# local runtime state
*.db

.DS_Store

# local self-signed TLS material for HAProxy dev testing (unused now Vercel handles TLS)
cert.pem
key.pem
